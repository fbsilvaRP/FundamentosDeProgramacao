/*Faça um programa que realize a operação x^y, digitados pelo usuário*/
#include <stdio.h>
int main()
{
	int op = 1; 
	while(op == 1)
	{
		int x, y; 
		printf("\n* * * Calculando x ^ y * * *");
		printf("\nDigite o valor de x: ");
		scanf("%d", &x); 
		printf("Agora, digite o valor de y: "); 
		scanf("%d", &y);
		//  ** ** * * *Continuar o pograma...   
	  
	}
}